dist
release
build
node_modules
package-lock.json
# Generated into the VS Code extension: compiler output and the copy of the
# built app it packages.
extensions/*/out
extensions/*/media/app
src/icon-names.generated.json
# Written by scripts/build-box-model.mjs and compared byte for byte by
# `npm run boxmodel:check`, so it must be exactly what the script emits.
src/render/boxModel.generated.ts
# Same arrangement for scripts/build-width-table.mjs and `npm run
# widthtable:check`, which only runs where the fonts are installed.
src/widthTable.generated.ts
THIRD-PARTY-NOTICES.md
*.tsbuildinfo
