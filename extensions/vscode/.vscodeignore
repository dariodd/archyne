# Sources and build inputs: the package ships `out/` and `media/app/`.
src/**
scripts/**
tsconfig.json
.gitignore
**/*.map
**/tsconfig.tsbuildinfo

# Maintainer notes, kept beside the project and out of both the repository and
# the package. `README.md` is the listing page and is the only prose that
# ships.
*.local.md

# The bundled editor carries its own demo page, which nothing here opens.
media/app/embed-demo.html
