# Sources and build inputs: the package ships `out/` and `media/app/`.
src/**
scripts/**
tsconfig.json
.gitignore
**/*.map
**/tsconfig.tsbuildinfo

# Maintainer notes. `README.md` is the listing page and ships; this is the
# file that exists so the listing does not have to carry any of it.
DEVELOPING.md

# The bundled editor carries its own demo page, which nothing here opens.
media/app/embed-demo.html
